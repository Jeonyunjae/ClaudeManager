# 개발 피드백

## 상태
⏳ 검토 전

## 피드백

## 기술 결정 이력

| 일자 | 결정 | 근거 |
|---|---|---|
| 2026-09-11 | 백업 덤프에 pg 커넥션 폴백 경로를 둔다 | 이 워크스테이션에 `pg_dump`(postgresql-client)가 없고 설치에 sudo 가 필요하다. DB 는 도커 안이라 컨테이너의 pg_dump 도 `docker exec`(=sudo) 없이는 못 쓴다. 폴백 덤프에는 스키마가 없어 복원 순서가 `db:migrate` → 덤프 적용이 된다 |
| 2026-09-11 | Main 단일성을 애플리케이션이 아니라 DB 제약으로 강제한다 | 조회→삽입 사이를 코드로 막으려면 잠금이 필요한데, 부분 유니크 인덱스 한 줄이면 동시 요청에서도 정확하다. 실제로 3ms 간격 중복이 발생했다 |
| 2026-09-11 | 팝업 기본 크기를 화면 비율로 잡되 기존 고정값을 하한으로 둔다 | 비율만 쓰면 작은 화면에서 지금보다 줄어든다 |
